---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/globals.css
  - src/lib/utils.ts
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - vitest.config.ts
  - tests/setup.ts
  - tests/schema.test.ts
  - tests/auth.test.ts
  - convex/.env
  - .env.local
autonomous: true
requirements:
  - AUTH-01

must_haves:
  truths:
    - "Running `npx vitest run` exits 0 (test scaffold exists and passes)"
    - "`npx convex dev --once` exits 0 (Convex project initialized and schema deployable)"
    - "All required npm packages are listed in package.json dependencies"
    - "shadcn/ui components exist at src/components/ui/"
    - "Tailwind v4 CSS-first config is in src/app/globals.css (no tailwind.config.js)"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies declared"
      contains: "convex"
    - path: "vitest.config.ts"
      provides: "Test runner configuration"
    - path: "tests/setup.ts"
      provides: "Shared test setup file"
    - path: "tests/schema.test.ts"
      provides: "Schema stub tests"
    - path: "tests/auth.test.ts"
      provides: "Auth stub tests"
    - path: "src/components/ui/button.tsx"
      provides: "shadcn button component"
  key_links:
    - from: "vitest.config.ts"
      to: "tests/setup.ts"
      via: "setupFiles configuration"
      pattern: "setupFiles.*setup"
---

<objective>
Bootstrap the InvoiceTracker project: scaffold a Next.js 16 app with App Router, install and initialize Convex, install Clerk and supporting libraries, initialize shadcn/ui, and set up the vitest test infrastructure.

Purpose: Creates the complete development environment that every subsequent plan depends on. No application logic is written here — only the tooling, dependencies, and test scaffold.
Output: Runnable Next.js 16 app with Convex dev project initialized, shadcn/ui components available, and a green vitest suite with stub tests for schema and auth.
</objective>

<execution_context>
@/Users/mateoherrera/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mateoherrera/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Next.js 16 app and install all Phase 1 dependencies</name>
  <files>package.json, .env.local (stub), convex/ (directory created by npx convex dev)</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (installation commands, exact package versions, anti-patterns)
    - .planning/research/STACK.md (version compatibility matrix)
  </read_first>
  <action>
Run the following commands in sequence from the project root `/Users/mateoherrera/Documents/coding/InvoiceTracker/`:

**Step 1 — Scaffold Next.js 16:**
```bash
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --yes
```
This creates: `package.json`, `tsconfig.json`, `next.config.ts`, `src/app/`, `src/lib/utils.ts`, `public/`, and installs `next@16.1.6`, `react@19.2.4`, `typescript@5.x`.

**Step 2 — Install Convex and initialize:**
```bash
npm install convex
```
Do NOT run `npx convex dev` interactively — it requires a browser login. Instead, create a stub `.env.local` with a placeholder so the app compiles:
```
NEXT_PUBLIC_CONVEX_URL=https://placeholder.convex.cloud
```
The real value is set during first `npx convex dev` run by the user (Step 5 of user_setup). Create the `convex/` directory at the project root (not inside `src/`):
```bash
mkdir -p convex
```

**Step 3 — Install Clerk and supporting libraries:**
```bash
npm install @clerk/nextjs@7.0.4 svix@1.88.0
```

**Step 4 — Install UI and utility libraries:**
```bash
npm install lucide-react@0.577.0 zod@4.3.6 date-fns@4.1.0
```

**Step 5 — Install dev dependencies:**
```bash
npm install -D vitest@latest @vitejs/plugin-react@latest @testing-library/react@latest jsdom@latest
```

**Step 6 — Initialize shadcn/ui:**
```bash
npx shadcn@latest init -d
```
If this fails due to Tailwind v4 validation (known bug — see 01-RESEARCH.md Pitfall 6), run with `--legacy-peer-deps` or use `--yes` flag.

**Step 7 — Add shadcn/ui components:**
```bash
npx shadcn@latest add button card input label
```

**IMPORTANT — packages NOT to install yet:**
- `stripe`, `resend`, `@react-email/*`, `@tanstack/react-table`, `@stripe/stripe-js` — these are for later phases
- Do NOT install `tailwindcss` manually — it's already included by `create-next-app`

After all steps, verify `package.json` contains:
- `"next": "^16.1.6"` (or `"16.x"`)
- `"convex": "^1.33.1"` (or similar)
- `"@clerk/nextjs": "^7.0.4"`
- `"svix": "^1.88.0"`
- `"vitest"` in devDependencies
  </action>
  <verify>
    <automated>cd /Users/mateoherrera/Documents/coding/InvoiceTracker && node -e "const p=require('./package.json'); const deps={...p.dependencies,...p.devDependencies}; ['next','convex','@clerk/nextjs','svix','vitest','lucide-react','zod'].forEach(d=>{if(!deps[d])throw new Error('Missing: '+d)}); console.log('All required packages present')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"next"` in dependencies (version starting with 16)
    - package.json contains `"convex"` in dependencies
    - package.json contains `"@clerk/nextjs"` in dependencies
    - package.json contains `"svix"` in dependencies
    - package.json contains `"vitest"` in devDependencies
    - `src/components/ui/button.tsx` exists (shadcn button component copied in)
    - `src/components/ui/card.tsx` exists
    - `src/components/ui/input.tsx` exists
    - `src/components/ui/label.tsx` exists
    - `convex/` directory exists at project root (not inside src/)
    - `.env.local` exists and contains `NEXT_PUBLIC_CONVEX_URL=`
  </acceptance_criteria>
  <done>All packages installed, shadcn/ui initialized with 4 components copied in, convex/ directory exists at root, stub .env.local created.</done>
</task>

<task type="auto">
  <name>Task 2: Create vitest configuration and Wave 0 test stubs</name>
  <files>
    vitest.config.ts,
    tests/setup.ts,
    tests/schema.test.ts,
    tests/auth.test.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-VALIDATION.md (Wave 0 requirements, test infrastructure spec)
    - package.json (to confirm vitest version installed)
  </read_first>
  <action>
Create the following four files exactly as specified:

**`vitest.config.ts`** (at project root):
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./tests/setup.ts'],
    include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx'],
    exclude: ['node_modules', '.next'],
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
});
```

**`tests/setup.ts`**:
```typescript
// Shared test setup for Phase 1 — Convex + Clerk mocks
// Extend as needed in later phases

// Suppress console.error noise in test output from Convex/Clerk provider warnings
const originalError = console.error;
beforeAll(() => {
  console.error = (...args: unknown[]) => {
    if (
      typeof args[0] === 'string' &&
      (args[0].includes('ClerkProvider') || args[0].includes('ConvexProvider'))
    ) {
      return;
    }
    originalError(...args);
  };
});

afterAll(() => {
  console.error = originalError;
});
```

**`tests/schema.test.ts`**:
```typescript
/**
 * Schema validation stubs — Phase 1
 * These tests verify that the Convex schema file exists and exports a valid schema.
 * Full data operation tests are added in Phase 2 when invoice mutations are built.
 */
import { describe, it, expect } from 'vitest';
import * as fs from 'fs';
import * as path from 'path';

describe('Convex schema', () => {
  it('schema.ts exists at convex/schema.ts', () => {
    const schemaPath = path.resolve(__dirname, '../convex/schema.ts');
    expect(fs.existsSync(schemaPath)).toBe(true);
  });

  it('schema.ts contains tenants table definition', () => {
    const schemaPath = path.resolve(__dirname, '../convex/schema.ts');
    if (!fs.existsSync(schemaPath)) {
      // Schema not yet created — this test will fail until Task 1 of Plan 01-02 runs
      expect.fail('convex/schema.ts does not exist yet — run plan 01-02 Task 1 first');
    }
    const content = fs.readFileSync(schemaPath, 'utf-8');
    expect(content).toContain('tenants');
  });

  it('schema.ts contains by_owner index', () => {
    const schemaPath = path.resolve(__dirname, '../convex/schema.ts');
    if (!fs.existsSync(schemaPath)) {
      expect.fail('convex/schema.ts does not exist yet');
    }
    const content = fs.readFileSync(schemaPath, 'utf-8');
    expect(content).toContain('by_owner');
  });

  it('schema.ts contains invoices table definition', () => {
    const schemaPath = path.resolve(__dirname, '../convex/schema.ts');
    if (!fs.existsSync(schemaPath)) {
      expect.fail('convex/schema.ts does not exist yet');
    }
    const content = fs.readFileSync(schemaPath, 'utf-8');
    expect(content).toContain('invoices');
  });
});
```

**`tests/auth.test.ts`**:
```typescript
/**
 * Auth flow stubs — Phase 1
 * These tests verify that key auth files exist and contain the required patterns.
 * Runtime auth behavior (sign-up, session persistence, isolation) must be verified manually
 * because Clerk + Convex require a live environment.
 */
import { describe, it, expect } from 'vitest';
import * as fs from 'fs';
import * as path from 'path';

describe('proxy.ts route protection', () => {
  it('proxy.ts exists at src/proxy.ts', () => {
    const proxyPath = path.resolve(__dirname, '../src/proxy.ts');
    expect(fs.existsSync(proxyPath)).toBe(true);
  });

  it('proxy.ts uses clerkMiddleware (not deprecated middleware)', () => {
    const proxyPath = path.resolve(__dirname, '../src/proxy.ts');
    if (!fs.existsSync(proxyPath)) {
      expect.fail('src/proxy.ts does not exist yet — run plan 01-03 Task 1 first');
    }
    const content = fs.readFileSync(proxyPath, 'utf-8');
    expect(content).toContain('clerkMiddleware');
    expect(content).not.toContain('export function middleware');
  });

  it('proxy.ts protects /dashboard route', () => {
    const proxyPath = path.resolve(__dirname, '../src/proxy.ts');
    if (!fs.existsSync(proxyPath)) {
      expect.fail('src/proxy.ts does not exist yet');
    }
    const content = fs.readFileSync(proxyPath, 'utf-8');
    // Public routes are listed; dashboard is NOT in the public list
    expect(content).toContain('sign-in');
    expect(content).toContain('sign-up');
  });
});

describe('getCurrentUserId helper', () => {
  it('convex/users.ts exists', () => {
    const usersPath = path.resolve(__dirname, '../convex/users.ts');
    expect(fs.existsSync(usersPath)).toBe(true);
  });

  it('getCurrentUserId function is exported from convex/users.ts', () => {
    const usersPath = path.resolve(__dirname, '../convex/users.ts');
    if (!fs.existsSync(usersPath)) {
      expect.fail('convex/users.ts does not exist yet — run plan 01-02 first');
    }
    const content = fs.readFileSync(usersPath, 'utf-8');
    expect(content).toContain('export async function getCurrentUserId');
    expect(content).toContain('identity.subject');
  });

  it('getCurrentUserId throws if identity is null (no unauthenticated degradation)', () => {
    const usersPath = path.resolve(__dirname, '../convex/users.ts');
    if (!fs.existsSync(usersPath)) {
      expect.fail('convex/users.ts does not exist yet');
    }
    const content = fs.readFileSync(usersPath, 'utf-8');
    expect(content).toContain('throw new Error');
    expect(content).toContain('Unauthenticated');
  });
});

describe('Tenant isolation contract', () => {
  it('convex/http.ts exists (Clerk webhook handler)', () => {
    const httpPath = path.resolve(__dirname, '../convex/http.ts');
    expect(fs.existsSync(httpPath)).toBe(true);
  });

  it('upsertFromClerk is an internalMutation (not publicly callable)', () => {
    const usersPath = path.resolve(__dirname, '../convex/users.ts');
    if (!fs.existsSync(usersPath)) {
      expect.fail('convex/users.ts does not exist yet');
    }
    const content = fs.readFileSync(usersPath, 'utf-8');
    expect(content).toContain('internalMutation');
    // Must NOT accept userId from client args — reads from db only
    expect(content).not.toMatch(/args:\s*\{[^}]*userId.*v\.string/);
  });
});
```

After writing these files, run:
```bash
cd /Users/mateoherrera/Documents/coding/InvoiceTracker && npx vitest run --reporter=verbose 2>&1
```
The `schema.test.ts` tests that check file contents will fail (convex/schema.ts doesn't exist yet — that's Plan 01-02). The `auth.test.ts` file-existence tests will also fail for the same reason. This is expected and correct — these are the RED tests that Plans 01-02 and 01-03 will turn GREEN.

The vitest runner itself must exit without a configuration error. If any import errors occur, fix the vitest.config.ts alias path.
  </action>
  <verify>
    <automated>cd /Users/mateoherrera/Documents/coding/InvoiceTracker && npx vitest run --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `vitest.config.ts` exists at project root
    - `vitest.config.ts` contains `setupFiles: ['./tests/setup.ts']`
    - `vitest.config.ts` contains `environment: 'jsdom'`
    - `tests/setup.ts` exists
    - `tests/schema.test.ts` exists and contains `describe('Convex schema'`
    - `tests/auth.test.ts` exists and contains `describe('proxy.ts route protection'`
    - `npx vitest run` launches without a configuration crash (even if individual tests fail due to missing files — that is expected at this wave)
    - `tests/auth.test.ts` contains the string `identity.subject` in the acceptance check
    - `tests/auth.test.ts` contains the string `internalMutation` in the acceptance check
  </acceptance_criteria>
  <done>vitest configured, four test files created (schema stubs + auth stubs), test runner launches without configuration errors.</done>
</task>

</tasks>

<verification>
After completing both tasks:
1. `node -e "require('./package.json')"` exits 0 — package.json is valid JSON
2. `ls src/components/ui/` shows button.tsx, card.tsx, input.tsx, label.tsx
3. `ls convex/` shows the directory exists at project root
4. `npx vitest run` launches (may show failures for missing files — expected at this stage)
5. `.env.local` contains `NEXT_PUBLIC_CONVEX_URL=`
</verification>

<success_criteria>
- Next.js 16 scaffold complete with TypeScript, Tailwind v4, App Router, and src/ directory structure
- All Phase 1 npm packages installed (convex, @clerk/nextjs, svix, lucide-react, zod, date-fns, vitest)
- shadcn/ui initialized with button, card, input, label components at src/components/ui/
- convex/ directory at project root (not inside src/)
- vitest.config.ts configured with jsdom environment and setupFiles
- Four test files exist: tests/setup.ts, tests/schema.test.ts, tests/auth.test.ts
- Test runner launches without configuration crashes
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md` following the summary template at `/Users/mateoherrera/.claude/get-shit-done/templates/summary.md`.

Key things to capture in the summary:
- Exact versions of packages actually installed (from package.json lock file)
- Whether shadcn init required any workarounds (Tailwind v4 validation bug)
- Any deviations from the planned installation sequence
- The placeholder NEXT_PUBLIC_CONVEX_URL value used
</output>
